const fs=require("fs");const p=require("path");const d=__dirname;function w(f,c){fs.writeFileSync(p.join(d,f),c,"utf8");console.log("Wrote "+f);}console.log("Generator ready - will create all pages");// Template parts stored in separate files - read themconst tpl={};try{  const files=fs.readdirSync(p.join(d,"scraped"));  files.forEach(function(f){tpl[f.replace(".json","")]=JSON.parse(fs.readFileSync(p.join(d,"scraped",f),"utf8"));});  console.log("Loaded "+Object.keys(tpl).length+" templates");}catch(e){console.error(e);}console.log("Ready to build");
